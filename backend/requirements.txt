# Flexible pins so pip can pick wheels for your Python (e.g. 3.12–3.14 on Windows).
fastapi>=0.115.0,<0.120.0
uvicorn[standard]>=0.30.0,<0.35.0
sqlalchemy>=2.0.30,<2.1.0
pydantic-settings>=2.5.0,<3.0.0
httpx>=0.27.0,<0.29.0
python-dateutil>=2.9.0
tenacity>=8.2.0,<10.0.0
python-dotenv>=1.0.0
apscheduler>=3.10.0,<4.0.0

# Real scraping stack
selectolax>=0.3.21       # fast HTML parser for non-Shopify PLP/PDP scraping
playwright>=1.46.0       # headless Chromium for SPA / session-based scraping

# Background queue (Redis-backed). RQ >=2.8 wraps fork Process init so imports work on Windows.
redis>=5.0.0,<6.0.0
rq>=2.8.0,<3.0.0
# Optional periodic enqueue helper — not imported by the API; install only if needed.
# rq-scheduler>=0.13.1

# Chat assistant (Gemini)
google-genai>=1.0.0,<2.0.0
